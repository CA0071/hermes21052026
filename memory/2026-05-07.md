# 2026-05-07

- 00:5X CST：按用户要求添加 Codex skill `hyperframe`。先读取 `/Users/yat/.hermes/skills/hyperframes` 现有资料，再用 skill-creator 初始化 `/Users/yat/.codex/skills/hyperframe`，复制原 HyperFrames 资料（references/scripts/templates/palettes 等），将 `SKILL.md` frontmatter 名称改为 `hyperframe` 并补充 HyperFrame/HyperFrames 触发描述，修正 `agents/openai.yaml` 的 default prompt 为 `Use $hyperframe ...`。验证：`quick_validate.py /Users/yat/.codex/skills/hyperframe` 输出 `Skill is valid!`；`sed` 回读 `SKILL.md` 与 `agents/openai.yaml`；`ls` 回读 skill、references、scripts 目录。
- 18:4X CST：用户追问 hyperframe 视频任务为什么跑十几个小时未推进，并要求继续完成。根因复盘：上一轮在 `/Users/yat` 全目录运行过宽 `rg --files | rg package.json/hyperframe`，扫到大量 `node_modules`、Library 受限目录和微信/缓存文件，输出爆炸并阻塞；随后又有未加引号的 URL 被 zsh `&` 切开、`npm view hyperframes version` 因 DNS/网络受限等待失败、Stooq CSV 端点要求 API key/captcha，导致链路没有及时退化。已完成预演版项目 `/Users/yat/hyperframes/nasdaq-waves-preview`：`index.html` 为 30 秒 1920x1080 单文件 HyperFrame 风格预演，主题为“纳斯达克指数走势对比：Netscape 互联网浪潮 vs ChatGPT AI 浪潮”；同时写入 `design.md`、`.hyperframes/expanded-prompt.md`、`data/README.md`。数据说明：当前曲线是归一化示意，不使用 Stooq 返回的 API key 提示伪装为行情数据。验证：`ls/find` 回读文件列表；`sed` 回读 design/expanded prompt/data README/index 片段；Node 抽取 `<script>` 后 `new Function` 通过并显示 `scripts_ok 1 / scenes 5 / duration30 true`；Python HTMLParser 显示 `html_parse_ok`、`title_ok True`；本地静态服务器 3468 通过提权启动后 `curl` 返回 `200 27431`，随后 `kill -TERM 69527` 关闭，`lsof -iTCP:3468` 无监听。
- 22:2X CST：修复 `/Users/yat/hermes-desktop-yat` 的 Yat 安装包在另一台 Mac 首次安装时报 `Bundled Hermes Agent source is available, but uv is not installed`。根因：App 包内有 Hermes 源码，但安装器要求目标机预装 `uv`；`uv` 是 Astral 的 Python 包/虚拟环境管理器，不是用户自己的文件。改动：`src/main/installer.ts` 新增 `installUvIfMissing()`，找不到 `uv` 时自动执行 `curl -LsSf https://astral.sh/uv/install.sh | sh`，安装后重新查找并继续 `uv venv` / `uv pip install`；更新 README、README.zh-CN、YAT_INSTALL、YAT_DELIVERY 说明。重新构建 `npm run build:mac`，更新 release manifest，验证 `npm run typecheck`、`npm run test`、`npm run verify:release:no-mount` 均通过。新产物：`/Users/yat/hermes-desktop-yat/dist/yat-0.3.2.dmg` SHA-256 `5bda69634f99242ebf7fa96a29a876b10a0b0684ffcd34a5ad7a090b982daa7b`；`/Users/yat/hermes-desktop-yat/dist/Yat-0.3.2-arm64-mac.zip` SHA-256 `ab9c67f83795354fb33b4ad85ce22f012ce0aa932604d205abc768981c4dac93`。提交：`7af4d57 fix: bootstrap uv during Yat install`。
- 22:5X CST：用户反馈 mmbook 仍卡在 uv 环节后，进一步把 macOS arm64 `uv` 直接打入 Yat 安装包。新增 `/Users/yat/hermes-desktop-yat/resources/uv/macos-arm64/uv`（uv 0.10.8, Mach-O arm64），安装器 `findUvCommand()` 优先查 `Yat.app/Contents/Resources/uv/macos-arm64/uv`，electron-builder `extraResources` 增加 `resources/uv -> uv`，release manifest/verify 脚本和测试增加包内 uv 条目校验。验证：`npm run typecheck && npm run test && npm run build:mac` 通过；打包后 `dist/mac-arm64/Yat.app/Contents/Resources/uv/macos-arm64/uv --version` 输出 `uv 0.10.8 (Homebrew 2026-03-03)`，`file` 显示 arm64，ZIP 内含该路径；`npm run manifest:release && npm run verify:release:no-mount` 通过。新 DMG SHA-256 `e993eeec32d70152fbb152c7f15366a0bb2c563be8de078d8fb0b92533f2a527`，ZIP SHA-256 `86ac279e4c9a4b88dfe8d6face91c0c13b99ea9ef7adbdc688cace03797f3649`。
